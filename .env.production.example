# ╔══════════════════════════════════════════════════════════════╗
# ║  ATLAS — Production Environment Variables                   ║
# ║  Copy to .env and fill in real values before deploying.     ║
# ╚══════════════════════════════════════════════════════════════╝

# ── Server ──
PORT=3001
NODE_ENV=production
CORS_ORIGINS=http://localhost,https://your-domain.com

# ── Anthropic (AI Intelligence Briefs) ──
# https://console.anthropic.com/ — required for AI brief generation
ANTHROPIC_API_KEY=sk-ant-...

# ── ACLED (Conflict data) ──
# https://developer.acleddata.com/ — free academic access
ACLED_EMAIL=your@email.com
ACLED_PASSWORD=your_password

# ── EIA (Oil/Energy data) ──
# https://www.eia.gov/opendata/ — free
EIA_API_KEY=your_key

# ── FRED (US Macro indicators) ──
# https://fred.stlouisfed.org/docs/api/ — free
FRED_API_KEY=your_key

# ── Congress.gov (Legislation tracking) ──
# https://api.congress.gov/ — free
CONGRESS_API_KEY=your_key

# ── OpenSky Network (Military flight tracking) ──
# https://opensky-network.org/ — free account
OPENSKY_CLIENT_ID=your_client_id
OPENSKY_CLIENT_SECRET=your_client_secret

# ── X/Twitter API (OSINT feeds) ──
# https://developer.x.com/ — Basic plan $100/mo
X_BEARER_TOKEN=your_bearer_token

# ── AlienVault OTX (Cyber threat intel) ──
# https://otx.alienvault.com/ — free
ALIENVAULT_API_KEY=your_key

